import 'package:flutter/material.dart';
import './enpty.dart';

class WaitUsedPage extends StatelessWidget {
  const WaitUsedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const EnptyPage();
  }
}
