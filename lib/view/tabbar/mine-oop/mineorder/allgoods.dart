import 'package:flutter/material.dart';
import './enpty.dart';

class AllGoodsPage extends StatelessWidget {
  const AllGoodsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('test');
    // return EnptyPage();
  }
}
