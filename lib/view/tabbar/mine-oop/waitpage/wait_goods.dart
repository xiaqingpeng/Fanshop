import 'package:flutter/material.dart';

class Waitgoods extends StatelessWidget {
  const Waitgoods({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('666');
  }
}
