import 'package:flutter/material.dart';
import './enpty.dart';

class HasUsedPage extends StatelessWidget {
  const HasUsedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const EnptyPage();
  }
}
